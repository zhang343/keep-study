<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuang.message.mapper.InfoCourseMapper">

    <select id="findUserCourseList" resultType="java.lang.String" >
        select course_id from info_course
        where user_id = #{userId}
        order by gmt_modified desc
        limit #{current} , #{limit}
    </select>

    <update id="setCourseRead" >
        update info_course set is_read = 1 where course_id in
        <foreach collection="courseIdList" item="courseId" open="(" close=")" separator="," >
            #{courseId}
        </foreach>
        and is_read = 0 and user_id = #{userId}
    </update>
</mapper>
