<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuang.bbs.mapper.ColumnMapper">


    <select id="findUserColumn" resultType="com.kuang.bbs.entity.vo.ColumnVo" >
        select id , title , views , vsibility , color from bbs_column
        where user_id = #{userId}
        order by gmt_create desc
    </select>

    <select id="findOtherUserColumn" resultType="com.kuang.bbs.entity.vo.ColumnVo" >
        select id , title , views , vsibility , color from bbs_column
        where user_id = #{userId} and is_release = 1
        order by gmt_create desc
    </select>
</mapper>
